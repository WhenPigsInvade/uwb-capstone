# Exawater Capstone Project

Repository for all code for Exawater.

# Setup

1. Install Docker
2. Copy .env over to the main directory
3. Run `docker compose up --build` while in the main directory, you should see it running in your list of containers

# Accessing the Service
You can access the service locally via `http://0.0.0.0:5000/`
